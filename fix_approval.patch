--- src/components/movement/ApprovalAndParts.tsx
+++ src/components/movement/ApprovalAndParts.tsx
@@ -361,6 +361,16 @@
     return true;
   });
 
+  useEffect(() => {
+    setDecisions(prev => {
+      let changed = false;
+      const next = { ...prev };
+      pendingInvoices.forEach(inv => {
+        if (inv.id && next[inv.id] === undefined) {
+          next[inv.id] = 'approved';
+          changed = true;
+        }
+      });
+      return changed ? next : prev;
+    });
+  }, [pendingInvoices]);
+
   const safeCurrentPage = Math.min(currentPage, Math.max(1, Math.ceil(pendingInvoices.length / itemsPerPage)));
   const totalPages = Math.max(1, Math.ceil(pendingInvoices.length / itemsPerPage));
