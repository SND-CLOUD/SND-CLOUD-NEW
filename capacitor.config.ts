import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snd.maintenance',
  appName: 'SND Maintenance System',
  webDir: 'dist'
};

export default config;
